name: q3-opt200-efficiency
on:
  push:
    branches: [q3-opt-200]
    paths:
      - 'tools/q3_bench_efficiency.py'
      - '.github/workflows/q3_opt200_efficiency.yml'
jobs:
  screen:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --disable-pip-version-check numpy
      - name: Restore exact offline simulator
        run: |
          python q3/bench/sim_payload.py
          python q3/bench/jammers_offline_sim.py selftest
      - name: Efficiency screen
        env:
          Q3_BENCH_K: '2'
          Q3_WORKERS: '7'
        run: python tools/q3_bench_efficiency.py
