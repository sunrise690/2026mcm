# 第一次本地迭代：外环路线压缩

此目录从 `iter_000_base` 的 v388 程序独立复制。使用同一个离线模拟器 v3 和固定种子做配对测试。

改动仅发生在 `workspace/code/live_q4.py`：保留中心与五个内环点的原顺序，对八个外环点以最近邻初始化、2-opt 改善开放路径。14 个发现点全部保留，定位、清除走廊、复测、补漏预算和停止条件未改动。

原名义普查路径为 17476.688m，新路径为 16316.691m，减少 1159.998m，按 5m/s 对应 232.000s。这里是纯普查几何路程，不能当作实测总时间收益；清除插入、机会复测和计数平台停止会受顺序影响。

尚需同种子 A/B 验证总时间、成功清除率、测量次数、失败清除次数。策略只使用 enter/measure/clear/exit 返回值；测试结束后才由评测器核算真值。
