# iter_081_defer_last_bootstrap_at15

继承 iter_078。中心和前三个800米环点扫描后，若公开反馈已发现至少15个频道，不再按固定顺序立即绕行到最后一个环点，而是把该点作为必访发现任务加入待清目标与联合探点的动态最近邻+2-opt路线。最后环点不会删除，未知频道仍按原逻辑测量，因此保留发现覆盖；仅改变高源数状态下的访问顺序。

策略只读取 found、belief 与当前位置，不读取真实源数、位置或隐藏真值。
