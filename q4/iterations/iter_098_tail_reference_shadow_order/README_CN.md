# iter_098_tail_reference_shadow_order

继承 iter_097。覆盖参考改为最后一个明确的后验/救援探索点 `tails[-1]`。普通清除结束后的机会扫描也会写入 visited，因此 visited[-1] 仍可能只是清除位置；tails 保留了真正决定剩余扇区覆盖的最后探索方向。

## 离线筛查结果

困难4例结果与 iter_096 相同：全清 3/4，平均 567.075 s/源，说明仅改变参考点不足以继续改善。

