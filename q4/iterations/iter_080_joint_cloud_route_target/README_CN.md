# iter_080_joint_cloud_route_target

继承 iter_078。对只有一条正示向的待清频道，路径排序不再使用保守扇形外包圆中心，而是使用全部公开正测量、no-signal点和清除失败点形成的联合可行云坐标中位点。该目标更接近后续走廊的预期清除位置，供动态最近邻与2-opt排序使用。

改动只影响频道访问顺序；严格可行域、走廊覆盖、清除动作和兜底逻辑保持不变，不读取隐藏真值。
